const problems = () => {
  
  const problems = document.querySelector('#problems');
  const problemsItem = problems.querySelectorAll('.problems-item');
  // console.log('problemsItem: ', problemsItem);
  const size = window.innerWidth;
  // if (size > 1024) {
  
  // } else {
  
  // }
  
};

export default problems;